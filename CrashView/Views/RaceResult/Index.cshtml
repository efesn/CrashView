@model IEnumerable<CrashView.Entities.RaceResult>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Race_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Person_ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PointsEarned)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Race_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Person_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PointsEarned)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RaceResult_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RaceResult_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RaceResult_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
